{
  "name": "psao-artifact",
  "version": "1.0.0",
  "private": true,
  "description": "Instrumentation and scenario services for the PSAO reproducibility artifact. The microbenchmark has its own manifest in bench/.",
  "_note_prom_client": "prom-client 15.x prints a deprecation notice pointing at @prometheus-io/client, its renamed successor. We stay on prom-client because that is what the paper's instrumentation is specified against and what the deployed images use; the API is the same if you migrate. See the comment in scenarios/s9-cluster-mode.js about the AggregatorRegistry constructor, which is required for cluster-mode metrics to work at all.",
  "scripts": {
    "s8": "node scenarios/s8-token-cache.js",
    "s9": "node scenarios/s9-cluster-mode.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "5.1.0",
    "jsonwebtoken": "^9.0.3",
    "prom-client": "^15.1.3"
  },
  "_note_express_pin": "express is PINNED to an exact 5.1.0, not a ^5.1.0 range. Measured on this testbed: express 5.2.1 collapses S5 at 600 rps where 5.1.0 holds target to 950 (123485 dropped iterations vs 10604), a ~35% loss of peak capacity. jsonwebtoken 9.0.3 / jws 4.0.1, which carries the HMAC signature-verification fix, costs nothing -- the two were isolated in separate runs. See data/runs/INDEX.md. Re-measure before widening this range."
}
